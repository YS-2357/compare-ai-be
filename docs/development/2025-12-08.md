# 2025-12-08 개발 로그

## 변경/구현 상세
- FastAPI 메타 정보 최신화
  - `app/config.py`: 기본 제목을 `Compare-AI Backend API`로 교체.
  - `app/main.py`: Description에 NDJSON 스트리밍/모델 오버라이드/사용량 헤더/관리자 bypass 내용 추가, 잘못된 따옴표 제거.
- 엔드포인트 문서화 강화
  - `app/api/routes.py`: `/api/ask` summary를 “LangGraph 스트리밍 질의 (NDJSON)”로 명시하고 200 partial 예시 및 401/503 응답 설명 추가.
  - `app/rate_limit/router.py`: `/usage`에 response_model과 예시(JSON), 401/503 응답 설명, summary 추가.
- 스키마 예시/설명 추가
  - `app/api/schemas/ask.py`: question/turn/max_turns/history/models 필드에 description+examples로 Example Value 노출.
  - `app/api/schemas/auth.py`: Register/Login 요청/응답 필드에 description+examples(이메일, 비밀번호, 토큰, 만료 등) 추가.
  - `app/api/schemas/common.py`: UsageResponse 필드에 examples 추가.
- 문서 인덱스/링크 갱신
  - `docs/README.md`: 한글화 및 최신 링크 2025-12-08로 교체.
  - `docs/changelog/README.md`, `docs/development/README.md`: 12-06/12-08 항목 반영.

## 검증/노트
- 로컬에서 의존성 스텁(`python-jose`, `email-validator` 대체)으로 OpenAPI 생성 후 `/docs` 스키마/예시 노출 확인. `uvicorn` 실 실행은 미진행.
- 실제 실행 시 `pip install python-jose email-validator` 후 `uvicorn app.main:app --reload` → `http://localhost:8000/docs`에서 Example Value/설명 노출 재확인 필요.
