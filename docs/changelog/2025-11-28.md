# 2025-11-28 변경 로그 (작성 예정)

- 계획: UI 리브랜딩(Compare-AI), 사용량 헤더 동기화, FE 분리/배포 준비, 인증 확장(비번 변경/탈퇴).
- 진행:
  - `/api/ask`에 사용량 헤더/summary 추가, UI 카운터 서버값 반영.
  - Streamlit 대화 로그/스트림 순서 표시, source 표시, FASTAPI_URL env 우선 사용.
  - Upstash 장애 시에도 로컬 캐시로 일일 3회 제한 유지.
  - Streamlit 로그아웃 시 `use_admin_bypass`/`admin_token` 세션 값을 초기화해 우회 상태가 남아 사용량 제한을 건너뛰는 문제 차단.
- 정리:
  - FE 레포에서 FastAPI/LLM용 모듈(api, services, prompts 등) 제거, Streamlit UI 최소 구성만 유지.
- 검증:
  - Upstash 일일 3회 제한 정상 동작 확인(추가 호출 시 429 응답).
  - 재로그인 시 초기 화면 사용량 표시가 무조건 3으로 보이는 잔여 UI 이슈가 남아 있음(추가 개선 예정).
- 후속: README/.env.example 업데이트, FE 호출 계약 정리, 인증 추가 라우트 구현.
